---
# Common role defaults

common_timezone: <your/Timezone>

common_upgrade_packages: true

common_packages:
  - curl
  - wget
  - git
  - htop
  - vim
  - unzip
  - tmux
  - rsync
  - jq
  - ca-certificates
  - gnupg
  - apt-transport-https

# Deploy user (optional - set in inventory)
# common_deploy_user: deployuser
# common_deploy_user_groups: [sudo]
# common_deploy_ssh_pubkey: "ssh-ed25519 AAAA..."
# common_deploy_user_nopasswd_sudo: true

# Hostname (optional - set in inventory)
# common_hostname: myserver
